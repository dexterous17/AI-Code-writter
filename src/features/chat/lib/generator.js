/*
 * Legacy manual component generator has been retired. The playground now relies
 * entirely on the AI chat workflow, so this module simply centralises shared
 * copy used elsewhere for consistency.
 *
 * Tip: Use the AI Chat tab to describe the component you want. The assistant
 * will generate runnable React code that appears in the editor and live preview.
 */

export const GENERATION_PROMPT_PLACEHOLDER = 'Describe the component you want to generate';

export const PLAYGROUND_TIP = "Tip: Use the AI Chat tab to describe the UI you need; the assistant updates the editor and preview for you.";

export function generateComponentFromPrompt() {
  // Manual heuristics were removed; rely on AI-based generation.
  return '';
}